class Greeter {
    greeting: string;
    constructor(message: string) {
        this.greeting = message;
    }
    greet() {
        return "Hello, " + this.greeting;
	}

     private getEndDate(startDate:Date, numDays:number)
     {
       let sevenDaysLater = startDate.addDays(numDays);
       let durationInMinutes = 15;
       let endDate = new Date( sev

     }     
}
